export const isAuthenticated = state => state.user;

export const getToken = state => state.token;

export const getUser = state => state.user;

export const getTotalBalance = state => state.totalBalance;
